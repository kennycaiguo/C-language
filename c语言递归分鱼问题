写法1：
#include <stdio.h>
#include<stdlib.h>

int sub(int n,int d) /*定义函数递归求鱼的总数*/
{
if(d==0) return 1;
if(n%5==1 && 5<n) return sub(n/5*4,d-1);
else return 0;
}
int main()
{ int i;
 for(i=1;!sub(i,5);i++); /*调用递归函数*/
 printf("The total number of fish is %d\n",i); //3121
 system("pause");
 return 0;
}
结果：3121

写法2，比较难理解：
#include <stdio.h>
int sub(int n) 											/*定义函数递归求鱼的总数*/
{
    if (n == 1)											/*当n等于1时递归结束*/
    {
        static int i = 0;
        do
        {
            i++;
        }
        while (i % 5 != 0);
        return (i + 1);									/*5人平分后多出一条*/
    }
    else
    {
        int t;
        do
        {
            t = sub(n - 1);
        }
        while (t % 4 != 0);
        return (t / 4 * 5+1);
    }
}
main() 
{ 
    int total; 
    total=sub(5); 										/*调用递归函数*/
    printf("The total number of fish is %d\n",total); //3121
	getchar();
    return 0;
}
结果：3121
