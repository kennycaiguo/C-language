c语言中字符串和字符数组的异同：
1.c语言其实并没有字符串，是用字符数组模拟的
2.字符串一定是字符数组，字符数组不一定是字符串
3.如果字符数组以'\0'结尾，那么这个字符数组就是一个字符串
如：char a[]={'a','b','c'}这个是字符数组
       char b[10]={'a','b','c','\0'} 这个是字符串
       char c[10]={'a','b','c',0} 这个也是是字符串，因为c语言中'\0'=0
